services:
  api_node:
    container_name: api_nestjs
    restart: always
    build:
      context: ./
    ports:
      - "3000:3000"
    networks:
      - rent_room_network
    env_file:
      - ./.env

networks:
  rent_room_network:
    driver: bridge